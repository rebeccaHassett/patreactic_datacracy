import styled from 'styled-components';
import { spacing, border, borderColor, palette } from '@material-ui/system';

const BoxMenu = styled.div`${spacing}${border}${borderColor}${palette}`;

export default BoxMenu;